import re
import psycopg2
from psycopg2 import pool

# --- 1. بيانات الاتصال ---
# نصيحة: في Render، يفضل وضع هذه القيم في Environment Variables للأمان
DB_URL = "postgresql://postgres.nmteaqxrtcegxmgvsbzr:mohammedfahdypb@aws-1-ap-south-1.pooler.supabase.com:6543/postgres"
BOT_TOKEN = "8498451295:AAGt1R7THllSjYtEe5hvIEPnPhRkS_iBcnU"

# متغير عالمي لمجمع الاتصالات
db_pool = None

# --- 2. إعدادات الأحياء الذكية (المدينة المنورة) ---
CITIES_DISTRICTS = {
    "المدينة المنورة": [
        "أبو مرخة", "أبيار علي", "الأسواف", "الإمام البخاري", "الإسكان", "البحر", 
        "البدراني", "الجمعة", "الجرف", "الحزام", "الحمراء", "الخاتم", "الخالدية", 
        "الدائرة", "الدائري", "الدويخله", "الدويمة", "الدعيثة", "الرانونا", "الرناونة", 
        "الربوة", "السيح", "الشروق", "الشرق", "الشهداء", "الشيبية", "الظاهرة", 
        "العاقول", "العالية مول", "العوالي", "العريض", "العزيزية", "العصبة", 
        "العنابس", "العنبرية", "العيون", "الفتح", "القبلتين", "القارات", "المبعوث", 
        "المطار", "المغيسله", "الملك فهد", "الملك فيصل", "المناخة", "المصانع", 
        "النور مول", "النبلاء", "النقيع", "النقمي", "الهجرة", "الواحة", "الوادي", 
        "بئر الماشي", "بئر عثمان", "باقدو", "بني حارثة", "بني ظفر", "تلال علي", 
        "جامعة طيبة", "الجامعة الإسلامية", "حديقة الملك فهد", "حمراء الأسد", 
        "دفاع", "ذو الحليفة", "رهط", "سيد الشهداء", "سلطانة", "سكة الحديد", 
        "السقيا", "شظاة", "شوران", "طريق الجامعات", "طريق الملك عبدالعزيز", 
        "طريق الهجرة", "طريق ينبع", "عروة", "قباء", "قربان", "محطة قطار الحرمين", 
        "مخطط التلال", "مستشفى الألماني", "مستشفى الحرس الوطني", "مستشفى الملك سلمان", 
        "مستشفى الملك فهد", "مستشفى المواساة", "مستشفى الولادة", "ممشى الهجرة", 
        "منار مول", "مهزور", "وعيرة"
    ]
}

# --- 3. وظائف قاعدة البيانات ---

def get_db_connection():
    """الحصول على اتصال آمن مع فحص حيويته (Health Check)"""
    global db_pool
    
    if db_pool is None:
        try:
            # تصحيح البروتوكول إذا لزم الأمر
            fixed_url = DB_URL.replace("postgres://", "postgresql://")
            # إنشاء مجمع اتصالات (بحد أدنى 1 وحد أقصى 20)
            db_pool = psycopg2.pool.ThreadedConnectionPool(1, 20, dsn=fixed_url, sslmode='require')
            print("✅ تم إنشاء مجمع اتصالات قاعدة البيانات.")
        except Exception as e:
            print(f"❌ فشل إنشاء المجمع: {e}")
            return None

    try:
        conn = db_pool.getconn()
        # فحص حيوية الاتصال قبل تسليمه
        try:
            with conn.cursor() as tmp_cur:
                tmp_cur.execute('SELECT 1')
        except (psycopg2.OperationalError, psycopg2.InterfaceError):
            print("🔄 إعادة بناء اتصال تالف...")
            db_pool.putconn(conn, close=True)
            return db_pool.getconn()
            
        return conn
    except Exception as e:
        print(f"⚠️ خطأ في سحب اتصال: {e}")
        return None

def release_db_connection(conn):
    """إعادة الاتصال للمجمع بدلاً من إغلاقه نهائياً"""
    global db_pool
    if not conn: return
    try:
        if db_pool:
            if not conn.closed:
                db_pool.putconn(conn)
        else:
            conn.close()
    except Exception as e:
        print(f"⚠️ خطأ أثناء تحرير الاتصال: {e}")
        try: conn.close() 
        except: pass

# --- 4. معالجة النصوص (الذكاء اللغوي) ---

def normalize_text(text):
    """تنظيف وتوحيد النصوص العربية لزيادة دقة البحث عن الأحياء والطلبات"""
    if not text: return ""
    
    # 1. تحويل للصغير وإزالة المسافات الزائدة
    text = text.strip().lower()
    
    # 2. إزالة التشكيل (فتحة، ضمة، إلخ)
    text = re.sub(r"[\u064B-\u0652]", "", text)
    
    # 3. توحيد الحروف المتشابهة
    replacements = {
        "أ": "ا", "إ": "ا", "آ": "ا",
        "ة": "ه", "ى": "ي", "ئ": "ي", "ؤ": "و"
    }
    for old, new in replacements.items():
        text = text.replace(old, new)

    # 4. إزالة تكرار الحروف (مشوااااار -> مشوار)
    text = re.sub(r'(.)\1+', r'\1', text)

    # 5. معالجة الـ التعريف (اختياري لجعل البحث مرناً)
    words = text.split()
    clean_words = []
    for w in words:
        # إذا كانت الكلمة تبدأ بـ "ال" وطولها أكثر من 3 حروف، نحذف "ال"
        if w.startswith("ال") and len(w) > 3:
            clean_words.append(w[2:])
        else:
            clean_words.append(w)

    return " ".join(clean_words)
